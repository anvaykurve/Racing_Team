# Racing_Team
IITB Racing Team Repository

ROS 2 uses colcon to build packages, but it is distributed as a separate Python tool. Most tutorials assume you have it, but it often needs that specific apt install command first.
I did not have it hence my ROS2 code was not running
